# 高等数学教材结构化处理管线 — 使用指南

> 工作目录：`F:\xschem\`（我的是这个,实际文件处理会自动替换工作目录）
> 涉及脚本：`prograhspilt.py` · `reprocess_original.py` · `run_all.py`

---

## 目录

1. [快速上手](#1-快速上手)
2. [整体流程概览](#2-整体流程概览)
3. [路径说明（重要）](#3-路径说明重要)
4. [前置准备](#4-前置准备)
5. [prograhspilt.py — 知识点切分](#5-prograhspiltpy--知识点切分)
6. [reprocess_original.py — 二次加工](#6-reprocess_originalpy--二次加工)
7. [run_all.py — 自动化流水线](#7-run_allpy--自动化流水线)
8. [更多使用场景](#8-更多使用场景)
9. [输出文件说明](#9-输出文件说明)
10. [参数速查表](#10-参数速查表)
11. [常见问题与排错](#11-常见问题与排错)

---

## 1. 快速上手

> 前提：已配置好 API Key（见[第4节](#4-前置准备)）

### 处理单个文件

```bash
cd F:\xschem

# 处理「二、函数的概念」这一个文件（切分 + 二次加工，一步到位）
python run_all.py -i "高等数学改/第一章 函数、极限、连续/第一节 映射与函数/二、函数的概念.md"
```

执行后的产出：

```
F:\xschem\
├── segments_output\                          ← 中间产物
│   ├── 二_函数的概念-segment.json            ← 结构化 JSON（紧凑版）
│   └── 二_函数的概念-segment_pretty.json     ← 结构化 JSON（格式化，方便检查）
│
├── processed_output\
│   └── 二_函数的概念\                        ← 最终产物目录
│       ├── 函数的定义.md
│       ├── 函数的有界性.md
│       ├── 例1.1.5.md
│       ├── ...                               ← 每个知识点一个文件
│       ├── _二_函数的概念_memory.json        ← 跨批次上下文日志
│       └── _二_函数的概念_report.md          ← 任务汇总报告
```

### 批量处理一整个目录
## 注意事项:
### 1.（建议把里面长的部分（“高等数学改\第二章 导数与微分\第一节 导数概念\第一节 导数概念.md”）以及一些例题（高等数学改\第二章 导数与微分\第一节 导数概念\习题2.1.md）都删了，只保留小节的md，比如“高等数学改\第二章 导数与微分\第一节 导数概念\二、导数定义.md”）;
### 2.如果发现一节下面只有一个文件，需检查md内部标题是否有公式，删掉公式保证能运行出结果
### 3.运行结果可以用dist检查，如果没有标题名，可能是因为没有处理llm输出的md包裹，需手动改
### 4.提交到github时，需检查生成的图片引用是否准确（可能是assets），不要提交报告和memory文件，但是memory记得保留下来，之后可能有用

```bash
# 递归处理「第一节 映射与函数」下所有 .md（自动跳过习题文件）
python run_all.py -i "高等数学改/第一章 函数、极限、连续/第一节 映射与函数"

# 递归处理整章
python run_all.py -i "高等数学改/第一章 函数、极限、连续"
```

### 只预览提示词，不花钱

```bash
python run_all.py -i "高等数学改/第一章 函数、极限、连续/第一节 映射与函数/二、函数的概念.md" --dry-run
```

---

## 2. 整体流程概览

```
原始教材 .md
      │
      ▼
 ┌──────────────────────────┐
 │   prograhspilt.py        │  ← 步骤 1：LLM 细粒度切分
 │   知识点语义分段         │
 └──────────────────────────┘
      │
      ▼  *-segment_pretty.json
      │
 ┌──────────────────────────┐
 │   reprocess_original.py  │  ← 步骤 2：LLM 二次加工
 │   生成独立 Markdown 文档 │
 └──────────────────────────┘
      │
      ▼  processed_output/{文件名}/
         多个独立的 .md 知识文档
```

- **`run_all.py`** 是总调度脚本，正常使用时**直接跑这一个即可**，它会自动串联步骤 1 和步骤 2。
- 也可以单独调用 `prograhspilt.py` 和 `reprocess_original.py`，适合需要中间检查 JSON 的场景。

---

## 3. 路径说明（重要）

### 相对路径 vs 绝对路径

三个脚本的路径处理方式略有不同：

| 脚本 | 相对路径基准 | 说明 |
|---|---|---|
| `run_all.py` | 自动以 `F:\xschem\` 为基准 | 第 55-56 行有 `if not p.is_absolute()` 的补全逻辑，无论从哪个目录执行都安全 |
| `prograhspilt.py` | **当前工作目录** | `--input` 传给 `open()` 直接读取，不会自动补全基准目录。单独调用时务必在 `F:\xschem\` 下运行，或传绝对路径 |
| `reprocess_original.py` | 当前工作目录 | 同上，所有路径直接传给 `open()` |

### 实际影响

```bash
# ✅ 安全 — 从 F:\xschem\ 运行，相对路径 OK
cd F:\xschem
python prograhspilt.py -i "高等数学改/第一章 函数、极限、连续/第一节 映射与函数/二、函数的概念.md"

# ✅ 安全 — 绝对路径不受工作目录影响
python prograhspilt.py -i "F:/xschem/高等数学改/第一章 函数、极限、连续/第一节 映射与函数/二、函数的概念.md"

# ⚠️ 有风险 — 从其他目录运行，相对路径会找不到文件
cd C:\
python F:/xschem/prograhspilt.py -i "高等数学改/第一章 函数、极限、连续/第一节 映射与函数/二、函数的概念.md"
# ❌ 实际查找的是 C:\高等数学改\...，找不到文件

# ✅ 安全 — 通过 run_all.py 调用则没有这个问题，它内部已做路径补全
python F:/xschem/run_all.py -i "高等数学改/第一章 函数、极限、连续/第一节 映射与函数/二、函数的概念.md"
```

**建议**：始终在 `F:\xschem\` 目录下运行，或一律使用绝对路径。

---

## 4. 前置准备

### 4.1 Python 环境

- 推荐使用 `F:\python\python.exe`（Python 3.12）
- 安装依赖：

```bash
pip install openai python-dotenv
```

### 4.2 API Key 配置

三个脚本都依赖阿里云 DashScope 的 `DASHSCOPE_API_KEY`。  
推荐在项目根目录创建 `.env` 文件（会自动被 `python-dotenv` 加载）：

```
# F:\xschem\.env
DASHSCOPE_API_KEY=sk-xxxxxxxxxxxxxxxxxxxxxxxx
```

也可以直接设置系统环境变量：

```powershell
# Windows PowerShell（仅当前会话有效）
$env:DASHSCOPE_API_KEY = "sk-xxxxxxxxxxxxxxxxxxxxxxxx"

# 永久写入系统环境变量
[System.Environment]::SetEnvironmentVariable("DASHSCOPE_API_KEY", "sk-xxx", "User")
```

> **注意**：三个脚本均在启动时校验 `DASHSCOPE_API_KEY`，未配置则立即报错退出。

### 4.3 辅助文件（可选）

| 文件 / 目录 | 路径 | 说明 |
|---|---|---|
| `目录.md` | `F:\xschem\目录.md` | 整本教材的章节目录，供 LLM 推断 source/section。缺失时跳过自动推断 |
| `markdown-hack规范.md` | `F:\xschem\markdown-hack规范.md` | 输出文档的格式规范，作为 system prompt 的一部分。缺失时跳过 |
| `samples/` 目录 | `F:\xschem\samples\` | 参考示例 `.md` 文件，供 LLM 参照输出格式。缺失时打印警告后继续 |

---

## 5. prograhspilt.py — 知识点切分

### 功能

调用 `qwen-plus` 大模型，将一份 Markdown 格式的教材内容按**细粒度、语义独立完整**的原则切分为多个知识点段落（segment），输出 JSON 数组。

### 基本用法

```bash
cd F:\xschem

# 使用默认路径（处理 一、集合和映射.md）
python prograhspilt.py

# 指定输入文件（相对路径，基于 F:\xschem\）
python prograhspilt.py -i "高等数学改/第一章 函数、极限、连续/第一节 映射与函数/二、函数的概念.md"

# 指定输入文件（绝对路径，从任何目录都能跑）
python prograhspilt.py -i "F:/xschem/高等数学改/第一章 函数、极限、连续/第一节 映射与函数/二、函数的概念.md"

# 同时指定输出文件名
python prograhspilt.py \
  -i "高等数学改/第一章 函数、极限、连续/第一节 映射与函数/二、函数的概念.md" \
  -o "函数的概念-segment.json"

# 手动指定 source 和 section（跳过自动推断，节省一次 API 调用）
python prograhspilt.py \
  -i "高等数学改/第一章 函数、极限、连续/第一节 映射与函数/二、函数的概念.md" \
  --source "第一章 函数、极限、连续/第一节 映射与函数/二、函数的概念.md" \
  --section "二、函数的概念"

# 关联章节数据库（可选，自动查找 section_id）
python prograhspilt.py \
  -i "..." \
  --chapter-db "F:/xschem/data/chapters.db"
```

### 参数说明

| 参数 | 简写 | 默认值 | 说明 |
|---|---|---|---|
| `--input` | `-i` | 硬编码默认 md 文件 | 输入 Markdown 文件路径（相对/绝对均可，见[路径说明](#3-路径说明重要)）|
| `--output` | `-o` | 自动根据 section 命名 | 输出 JSON 文件路径 |
| `--source` | `-s` | 自动推断 | JSON 中 `source` 字段的值 |
| `--section` | 无 | 自动推断 | JSON 中 `section` 字段的值 |
| `--section-id` | 无 | `null` | JSON 中 `section_id` 字段的值 |
| `--chapter-db` | 无 | `None`（不查库） | 章节 SQLite 数据库路径 |

### 自动推断逻辑

若不指定 `--source` 和 `--section`，脚本会额外发起一次 LLM 调用，用教材内容（前 600 字符）和 `目录.md` 推断当前文件属于哪个小节。

**建议**：处理大批量文件时，手动指定 `--source` / `--section` 可节省 API 费用并提升准确性。

### 输出内容

1. `{section}-segment.json` — 紧凑 JSON，直接可入库
2. `{section}-segment_pretty.json` — 格式化美化版，便于人工检查
3. `debug_response.txt` — LLM 原始响应（每次覆盖写入，用于排错）

### 每个 segment 的字段

| 字段 | 类型 | 说明 |
|---|---|---|
| `source` | string | 来源文件相对路径 |
| `section` | string | 所属小节名 |
| `section_id` | string\|null | 章节表 ID |
| `position` | int | 本小节内顺序号（从 1 开始） |
| `title` | string | 知识块标题（可为空）|
| `content` | string | LLM 精简后的核心内容摘要 |
| `raw_content` | string | 原文内容（保持 LaTeX 原样）|
| `type` | string | `concept`/`theorem`/`proof`/`example`/`note` |
| `aliases` | array | 概念同义词（仅 concept/theorem 类型提取）|
| `entities` | string | 逗号分隔的关键实体名词 |
| `references` | array | 含关系词的原文语句 |
| `confidence` | float | 提取置信度（0~1）|
| `needs_review` | bool | 置信度 < 0.8 时为 true |
| `summary` | string | 一句话摘要 |
| `created_at` | string | ISO 时间字符串 |
| `model` | string | 使用的模型名 |
| `version` | int | 版本号（固定为 1）|
| `embedding` | null | 预留向量字段 |

---

## 6. reprocess_original.py — 二次加工

### 功能

接收原始教材 `.md` + 切分得到的 JSON，调用 LLM 将每个 segment 转换为**符合 markdown-hack 规范的独立 Markdown 知识文档**，支持批次处理和跨批次上下文传递。

### 基本用法

```bash
cd F:\xschem

# 指定输入文件（使用切分步骤产出的 _pretty.json）
python reprocess_original.py \
  --input-md "高等数学改/第一章 函数、极限、连续/第一节 映射与函数/二、函数的概念.md" \
  --input-json "segments_output/二_函数的概念-segment_pretty.json"

# 指定输出目录
python reprocess_original.py \
  --input-md "高等数学改/第一章 函数、极限、连续/第一节 映射与函数/二、函数的概念.md" \
  --input-json "segments_output/二_函数的概念-segment_pretty.json" \
  --output-dir "processed_output/二_函数的概念"

# 调整批次大小（每批处理 6 个 segment）
python reprocess_original.py \
  --input-md "..." --input-json "..." \
  --batch-size 6

# 干跑模式（只生成 prompt，不调用 API）
python reprocess_original.py --dry-run
```

### 参数说明

| 参数 | 简写 | 默认值 | 说明 |
|---|---|---|---|
| `--input-md` | `-m` | 硬编码默认 md 文件 | 原始教材 Markdown 文件路径 |
| `--input-json` | `-j` | `segments_output.json` | 切分 JSON 文件路径（推荐用 `*_pretty.json`）|
| `--spec-file` | `-s` | `F:\xschem\markdown-hack规范.md` | markdown-hack 规范文件路径 |
| `--samples-dir` | 无 | `F:\xschem\samples\` | 参考示例目录 |
| `--output-dir` | `-o` | `processed_output/{源文件名}/` | 输出目录 |
| `--batch-size` | `-b` | `4` | 每批处理的 segment 数量 |
| `--dry-run` | 无 | false | 只生成提示词，不调用 API |

### 批次处理机制

由于单次 LLM 输入/输出有 token 限制，脚本将所有 segments 按 `batch_size` 分批：

- **每批独立调用一次 LLM**，每批产出 `batch_size` 个 Markdown 文档
- **Memory 上下文**：后续批次会携带前面批次生成的文档列表（文件名 + 标题 + 摘要），让 LLM 能正确生成跨批次的 `[[引用]]` 链接
- **自动 Rewrite**：若某批输出的文档数量与预期不符，脚本会自动发起二次 LLM 调用修复分隔符问题

### 输出文件结构

每个输出 Markdown 文档包含：

```markdown
---
title: 函数的定义
numeric_source: 1-1-2-3    # 章-节-小节-序号
aliases:
  - 映射到实数集
  - 函数关系
tags:
  - 函数
  - 定义域
source: 第一章/第一节/二、函数的概念.md
position: 3
---

# 函数的定义

...正文内容（含 LaTeX 公式、表格、图片、语义块）...
```

还会在输出目录生成以下辅助文件：

| 文件 | 说明 |
|---|---|
| `_debug_{批次}_raw.txt` | LLM 原始响应（调试用） |
| `_debug_{批次}_rewrite.txt` | Rewrite 修正后的响应（触发时才有）|
| `_debug_{批次}_prompt.txt` | dry-run 时生成的提示词文件 |
| `_{来源文件}_memory.json` | 跨批次 memory 日志（存档用）|
| `_{来源文件}_report.md` | 本次任务汇总报告 |

---

## 7. run_all.py — 自动化流水线

### 功能

`run_all.py` 是总调度脚本，自动串联步骤 1（`prograhspilt.py`）和步骤 2（`reprocess_original.py`），支持**单文件**和**批量目录**两种模式。目录模式使用 `rglob` 递归搜索所有子目录。

### 基本用法

```bash
cd F:\xschem

# 处理单个文件（完整流程：切分 → 二次加工）
python run_all.py -i "高等数学改/第一章 函数、极限、连续/第一节 映射与函数/二、函数的概念.md"

# 处理整个小节目录（递归查找所有 .md，自动跳过习题文件）
python run_all.py -i "高等数学改/第一章 函数、极限、连续/第一节 映射与函数"

# 处理整章（递归遍历第一章下所有子目录）
python run_all.py -i "高等数学改/第一章 函数、极限、连续"

# 处理整本教材
python run_all.py -i "高等数学改"
```

### 参数说明

| 参数 | 简写 | 默认值 | 说明 |
|---|---|---|---|
| `--input` | `-i` | 无（必填） | 输入 `.md` 文件或目录路径（相对/绝对均可）|
| `--output-root` | `-o` | `processed_output/` | 最终文档输出根目录 |
| `--segments-dir` | `-s` | `segments_output/` | 中间 JSON 存放目录 |
| `--batch-size` | `-b` | `4` | 每批处理的 segment 数量 |
| `--dry-run` | 无 | false | 只生成提示词，不调用 LLM API |

### 执行逻辑

对每一个待处理的 `.md` 文件：

1. **步骤 1**：调用 `prograhspilt.py --input <file>` 进行知识点切分
   - 若 `segments_output/<文件名>-segment.json` 已存在，**跳过切分**，直接使用缓存
   - 切分成功后，将生成的 JSON 文件移入 `segments_output/` 目录
2. **步骤 2**（非 dry-run 时）：调用 `reprocess_original.py` 进行二次加工
   - 输出到 `processed_output/{文件名}/`
3. 多文件处理时，每个文件之间间隔 3 秒，避免触发 API 速率限制

### 跳过习题文件

文件名中包含 `习题`、`练习`、`作业` 的 `.md` 文件会被自动跳过（如 `习题1.1.md`）。

### 目录模式注意事项

使用目录路径时，脚本会**递归搜索所有 `.md` 文件**（使用 `rglob`）并按路径排序后依次处理。对于大目录（如整本教材），处理时间可能很长，建议先用 `--dry-run` 确认文件列表，再正式运行。

---

## 8. 更多使用场景

### 场景 A：先切分检查 JSON，再二次加工

```bash
cd F:\xschem

# 步骤 1：只切分
python prograhspilt.py \
  -i "高等数学改/第一章 函数、极限、连续/第一节 映射与函数/二、函数的概念.md" \
  -o "二_函数的概念-segment.json"

# 人工打开 二_函数的概念-segment_pretty.json 检查内容...

# 步骤 2：检查没问题后，二次加工
python reprocess_original.py \
  --input-md "高等数学改/第一章 函数、极限、连续/第一节 映射与函数/二、函数的概念.md" \
  --input-json "二_函数的概念-segment_pretty.json" \
  --output-dir "processed_output/二_函数的概念"
```

---

### 场景 B：JSON 已存在，只重跑二次加工

```bash
cd F:\xschem

# run_all.py 检测到 segments_output/ 中已有 JSON，会自动跳过步骤 1
python run_all.py -i "高等数学改/第一章 函数、极限、连续/第一节 映射与函数/二、函数的概念.md"

# 若想强制重新切分，先删除已有 JSON
del "segments_output\二_函数的概念-segment.json"
del "segments_output\二_函数的概念-segment_pretty.json"
python run_all.py -i "高等数学改/第一章 函数、极限、连续/第一节 映射与函数/二、函数的概念.md"
```

---

### 场景 C：只重跑步骤 2（手动调用 reprocess_original.py）

```bash
cd F:\xschem

python reprocess_original.py \
  --input-md "高等数学改/第一章 函数、极限、连续/第一节 映射与函数/二、函数的概念.md" \
  --input-json "segments_output/二_函数的概念-segment_pretty.json" \
  --output-dir "processed_output/二_函数的概念"
```

---

### 场景 D：批量处理整章，调整批次大小

```bash
cd F:\xschem

python run_all.py \
  -i "高等数学改/第一章 函数、极限、连续" \
  --batch-size 3
```

---

### 场景 E：先干跑预览提示词

```bash
cd F:\xschem

# 干跑（不花钱，只生成提示词文件）
python run_all.py \
  -i "高等数学改/第一章 函数、极限、连续/第一节 映射与函数/二、函数的概念.md" \
  --dry-run

# 检查 processed_output/二_函数的概念/ 下的 _debug_*_prompt.txt
# 确认提示词内容满意后，去掉 --dry-run 正式运行
python run_all.py \
  -i "高等数学改/第一章 函数、极限、连续/第一节 映射与函数/二、函数的概念.md"
```

---

## 9. 输出文件说明

### 目录结构

```
F:\xschem\
├── segments_output\                          ← 步骤1输出（中间 JSON）
│   ├── 二_函数的概念-segment.json
│   ├── 二_函数的概念-segment_pretty.json
│   ├── 三_函数的几种特性-segment.json
│   └── ...
│
├── processed_output\                         ← 步骤2输出（最终 Markdown）
│   ├── 二_函数的概念\
│   │   ├── 函数的定义.md
│   │   ├── 函数的有界性.md
│   │   ├── 例1.1.5.md
│   │   ├── _二_函数的概念_memory.json
│   │   └── _二_函数的概念_report.md
│   └── 三_函数的几种特性\
│       ├── ...
│
└── debug_response.txt                        ← prograhspilt.py 的最新 LLM 原始响应
```

### 生成的 Markdown 文档规范

每个文档都遵循 `markdown-hack` 规范，具备：

- **YAML frontmatter**：title、numeric_source（编号）、aliases、tags、source、position
- **一级标题**（不含编号前缀）
- **正文**：可含 LaTeX 公式（`$...$` / `$$...$$`）、图片、HTML 表格
- **语义块**（Pandoc fenced div）：
  - 例题：`::: {.example label="..." type=example}` ... `:::`
  - 证明：`::: {#ID .proof label="..." type=proof}` ... `:::`
  - 说明：`::: {.note}` ... `:::`
- **双向引用**：`[[目标文件名]]{type=..., label=..., render=link, tags=...}`

---

## 10. 参数速查表

### run_all.py

```
python run_all.py [选项]

  -i, --input          输入 .md 文件或目录（必填，相对/绝对路径均可）
  -o, --output-root    最终文档输出根目录（默认: processed_output/）
  -s, --segments-dir   中间 JSON 目录（默认: segments_output/）
  -b, --batch-size     每批 segment 数量（默认: 4）
      --dry-run        只生成提示词，不调用 API
```

### prograhspilt.py

```
python prograhspilt.py [选项]

  -i, --input         输入 Markdown 文件（相对/绝对路径均可）
  -o, --output        输出 JSON 文件路径（默认自动命名）
  -s, --source        source 字段（留空则自动推断）
      --section       section 字段（留空则自动推断）
      --section-id    section_id 字段（默认: null）
      --chapter-db    章节数据库路径（可选）
```

### reprocess_original.py

```
python reprocess_original.py [选项]

  -m, --input-md      原始教材 Markdown 文件
  -j, --input-json    切分 JSON 文件
  -s, --spec-file     markdown-hack 规范文件
      --samples-dir   参考示例目录（默认: samples/）
  -o, --output-dir    输出目录
  -b, --batch-size    每批 segment 数量（默认: 4）
      --dry-run       只生成提示词，不调用 API
```

---

## 11. 常见问题与排错

### Q1：运行提示 `ValueError: 请设置环境变量 DASHSCOPE_API_KEY`

在项目根目录创建 `.env` 文件并写入：
```
DASHSCOPE_API_KEY=sk-xxxxxxxx
```
或在当前 shell 中执行 `set DASHSCOPE_API_KEY=sk-xxxxxxxx`（Windows cmd）。

---

### Q2：`prograhspilt.py` 解析 JSON 失败，提示 `JSONDecodeError`

LLM 输出的 JSON 包含未转义的 LaTeX 反斜杠，脚本有自动修复逻辑（最多重试 3 次）。  
若仍失败，查看 `debug_response.txt` 中的原始输出，手动定位问题字符。

---

### Q3：`reprocess_original.py` 生成的文档数量比预期少

触发了 Rewrite 流程但仍未修复。查看 `_debug_{批次}_rewrite.txt`，对比原始 raw 文件，手动拆分或合并。可以减小 `--batch-size`（如改为 2）让 LLM 每次处理更少的 segment，减少分隔符出错概率。

---

### Q4：运行速度很慢

正常，每次 LLM API 调用需 30~90 秒。  
每批处理消耗的 token 约 4000~8000，费用估算会实时打印在控制台。  
处理大文件时，建议先用 `--dry-run` 预览提示词，确认无误再正式运行。

---

### Q5：想重新处理某个文件，但步骤 1 被跳过

`run_all.py` 会检测 `segments_output/` 中是否已有对应 JSON，若存在则跳过切分。  
手动删除该 JSON 文件后再运行即可强制重新切分：

```bash
cd F:\xschem
del "segments_output\二_函数的概念-segment.json"
del "segments_output\二_函数的概念-segment_pretty.json"
python run_all.py -i "高等数学改/第一章 函数、极限、连续/第一节 映射与函数/二、函数的概念.md"
```

---

### Q6：Windows 路径问题

- Python 脚本内部已做 `\` / `/` 的兼容处理，传入命令行参数时两者皆可。
- 若路径含空格，务必用引号括起来：`-i "高等数学改/第一章 函数、极限、连续"`
- 不要用中文引号 `""` ，只用英文双引号 `""`。
- `run_all.py` 内部有路径补全逻辑，从任何目录执行都安全。`prograhspilt.py` 和 `reprocess_original.py` 单独调用时，建议始终在 `F:\xschem\` 下运行，或使用绝对路径（见[路径说明](#3-路径说明重要)）。

---

### Q7：`samples/` 目录为空或不存在

不影响核心功能，脚本会打印警告后继续执行，只是 LLM 没有示例参考，输出格式稳定性稍低。  
建议放入 1~3 个符合 markdown-hack 规范的高质量示例 `.md` 文件。

---

### Q8：如何只重跑步骤 2（已有 JSON，重新生成文档）？

直接调用 `reprocess_original.py`，手动指定已有的 JSON 文件：

```bash
cd F:\xschem
python reprocess_original.py \
  --input-md "高等数学改/第一章 函数、极限、连续/第一节 映射与函数/二、函数的概念.md" \
  --input-json "segments_output/二_函数的概念-segment_pretty.json" \
  --output-dir "processed_output/二_函数的概念"
```

---

*文档最后更新：2026-05-12*
